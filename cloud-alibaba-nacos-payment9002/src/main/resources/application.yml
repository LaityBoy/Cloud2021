server:
  port: 9002
spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.174.131:1111 #配置nginx地址,之后由nginx转发
management:
  endpoints:
    web:
      exposure:
        include: "*"